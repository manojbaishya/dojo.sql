```sql
SELECT
    c.FirstName,
    c.LastName,
    c.Address,
    c.FirstName || ' ' || c.LastName || ', ' || c.Address || ', ' || c.City || ', ' || c.State || ', ' || c.PostalCode AS [Mailing Address]
FROM Customer AS c 
WHERE c.Country = 'USA';
```