### JOIN

Line Segment with two endpoints:

* Primary Key - on the table containing the main object/record data
* Foreign Key - on the other table that references the record


```sql
SELECT *
FROM Invoice AS i
INNER JOIN Customer AS c 
	ON i.CustomerId = c.CustomerId
ORDER BY
	c.CustomerId ASC;
```

