### Calculate average invoice amount in each billing city.

```sql
SELECT 
    i.BillingCity AS [Billing City],
    ROUND(AVG(i.Total), 2) AS [Average Purchase] 
FROM 
    Invoice AS i
-- WHERE [Billing City] IS NOT NULL
GROUP BY 
    [Billing City]
ORDER BY 
    [Average Purchase] DESC;
```