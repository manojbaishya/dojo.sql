```sql
WITH sold_tracks AS (
    SELECT
        DISTINCT TrackId
    FROM
        InvoiceLine
    ORDER BY 
        TrackId ASC
)
SELECT 
    t.TrackId,
    t.Name,
    t.Composer
FROM Track AS t
    INNER JOIN sold_tracks
WHERE
    t.TrackId NOT IN sold_tracks;
```