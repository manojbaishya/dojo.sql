- COUNT
- SUM
- AVG
- MIN
- MAX


```sql
SELECT 
    COUNT(i.Total) AS [Number of Sales], 
    SUM(i.Total) AS [Total Amount of Sales],
    AVG(i.Total) AS [Average Sales],
    MAX(i.Total) AS [Maximum Puchase],
    MIN(i.Total) AS [Minimum Purchase] 
FROM Invoice AS i;
```