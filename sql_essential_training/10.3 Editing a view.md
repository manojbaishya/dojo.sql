```sql
DROP VIEW V_AvgTotal;
CREATE VIEW V_AvgTotal AS
SELECT 
    ROUND(AVG(i.Total), 2) AS [Average Sales],
    ROUND(MAX(i.Total), 2) AS [Maximum Sale],
    ROUND(MIN(i.Total), 2) AS [Minimum Sale]
FROM
    Invoice AS i;
```