```sql
SELECT
    c.FirstName,
    c.LastName,
    c.Address,
    c.FirstName || ' ' || c.LastName || ', ' || c.Address || ', ' || c.City || ', ' || c.State || ', ' || SUBSTR(c.PostalCode, 1, 5) AS [Mailing Address]
FROM Customer AS c 
WHERE c.Country = 'USA';
```